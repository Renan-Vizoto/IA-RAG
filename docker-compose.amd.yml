services:
  ollama:
    # Gemma 4 GGUFs require Ollama >= 0.20.0 (llama.cpp gemma4 arch).
    image: ollama/ollama:0.20.6-rocm
    devices:
      - /dev/kfd
      - /dev/dri
    group_add:
      - video
    environment:
      HSA_OVERRIDE_GFX_VERSION: "11.0.0"